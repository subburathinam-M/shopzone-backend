server:
  port: ${SERVER_PORT:8761}

spring:
  application:
    name: EUREKA-SERVER

eureka:
  client:
    enabled: false
    register-with-eureka: false

    fetch-registry: false

logging:
  level:
    com.netflix.eureka: ${LOG_LEVEL:INFO}